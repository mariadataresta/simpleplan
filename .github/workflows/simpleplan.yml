name: Test Save File
on: workflow_dispatch
permissions: write-all
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "test_$(date)" > test.txt
      - run: |
          git add test.txt
          git commit -m "test" 
          git push
