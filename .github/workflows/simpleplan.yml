name: HRTG
on: 
  workflow_dispatch:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  create:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Create file
        run: ${{ secrets.NAMA1 }}
      
      - name: Setup Git
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
      
      - name: Push to repo
        run: |
          git add nama_found_git_config.txt
          git commit -m "🔄 Update otomatis $(date '+%d-%m-%Y %H:%M')" 
          git push
